import { NameFilterTwoPipe } from './name-filter-two.pipe';

describe('NameFilterTwoPipe', () => {
  it('create an instance', () => {
    const pipe = new NameFilterTwoPipe();
    expect(pipe).toBeTruthy();
  });
});
